OneTime is an encryption program that uses the one-time pad method.

Run `./onetime --intro` for an introduction, or `./onetime --help` for
complete usage.

See http://www.red-bean.com/onetime/ for more information.
